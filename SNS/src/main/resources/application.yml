server:
  port: 8090

spring:
  application:
    name: sps-sns
  profiles:
    active: production
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql-sns}:3306/sps_sns_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ${DB_PASS:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    "[co.sps.sns]": INFO
    "[org.springframework.web]": WARN
